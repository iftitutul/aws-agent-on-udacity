# Python
__pycache__/
*.py[cod]
venv/
.venv/
.pytest_cache/
*.egg-info/

# AWS / project state — regenerated by setup_gateway.py and create_harness.py.
# Tracked (not ignored): a reviewer explicitly asked for it as evidence of the harness and
# gateway ARNs, and relying on a manual "remember to copy it into the zip" step kept losing it.
# It's a training/lab account (voclabs), so the account ID inside these ARNs is not sensitive.
.aws-sam/
packaged.yaml

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local secrets
.env
*.pem
credentials
